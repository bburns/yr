// ui
// handle ui state - dialogs visible, etc.

import types from './types';
import * as actions from './actions';
import reducer from './reducer';
import selectors from './selectors';

export default { types, selectors, actions, reducer };
