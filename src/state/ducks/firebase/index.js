// firebase
// handle firebase state - logd-in user, etc.

import * as actions from './actions';
import reducer from './reducer';
import selectors from './selectors';

export default { selectors, actions, reducer };
